# migrated data copy (rebuildable from the source memory store via migrate.py) , not source
brain-new.db
notes-export.jsonl
migration-full.log
calibration-receipt.json
cutover-receipt.json
__pycache__/
*.pyc
source-memory.db
# the CoderWorker's confined write sandbox (governed artifacts, not source)
_scratch/
brain_bridge.py       # private crossover wiring to the real brain, not part of the public foundation
crossover.py          # private crossover runner (live guards + real brain), not part of the public foundation

# private cross-launch list (your real tool URLs); ship apps.json.example instead
apps.json

# local playwright venv for the Galileo browser-layer (rebuildable: uv venv .labenv && uv pip install playwright)
.labenv/
